services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    image: node-starter-img
    container_name: node-stater-app
    restart: on-failure:3
    env_file:
      - .env.prod
    ports:
      - '3000:3000'
